[workspace]
resolver = "2"
members = [
    "app",
    "core",
    "desktop",
    "mobile",
    "web",
]

[workspace.dependencies]
dioxus = { version = "0.7.9" }
lattis = { path = "app" }
lattis-core = { path = "core" }
